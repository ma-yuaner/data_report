# 综合分析：Hive ADS实际数据

来源：用户2026-09-18确认ADS清洗完成，明确要求切换Hive、不再预览；部门沿用此前暂不纳入要求。
状态：数据源与部门范围已确认；接入已开发。财务确认阶段、产品业务字典、唯一票粒度与历史回补完整性仍待业务/财务验证。
更新日期：2026-09-18。

## 页面与接口

业务分析 → 综合分析，路由 `/analysis/comprehensive`。接口 `/api/v1/analysis/comprehensive`。
固定读取当前 `HIVE_DATABASE`（默认lywz）下 `ads_business_profit_dimension_day`，不改变全局MySQL默认值、经营总览、专题或风控利润核对。

默认Asia/Shanghai今日，提供昨日、本月、本年、自定义，结束日期含当天，最多366天。按平台、站点、航司、平台内机票产品原值组合筛选；部门暂不纳入。筛选值保留平台命名空间及NULL，未知产品单列但保留业务金额，不推定公布价/私有价等分类。

页面展示四业务源记录数、业务估算利润、合计、业务日趋势、业务构成与可切换维度的分页对比。分页不截取汇总。分析此项保留原筛选并增加条件；日汇总抽屉查询同一时间/维度范围，不生成订单号或原因证据。

## 取数与质量

只读一条按dt分区范围限制的Hive ADS查询；不扫描四张DWD，不重复利润核算。当前MVP在服务端用Decimal汇总ADS记录，API金额为十进制字符串，浏览器仅格式化和绘图，不计算总额。单次超过10万条ADS记录拒绝发布而不是默默取Top N，后续大范围可改为数据库端聚合；不是该容量之外都已验证通过。

发布结果前逐日验证四业务coverage、单一etl_run_id、v1版本、业务日与dt一致、主键及源记录计数、缺失利润计数、已知/完整利润一致性。缺分区或缺标记不会当作零业务；范围中任一日期未加工，不发布整个范围的完整汇总，提示回补或缩小范围。已有日期可在coverage中查看。

缺失利润仍显示数量和缺失记录数，完整利润及合计为NULL，已知金额单列；全缺失已知金额也为NULL。四业务覆盖完整且没有选中记录时才显示真实零。查询失败清空旧结果，不回退到演示数据。

数量各业务分列，不合并为票数，不计算未经确认的退票率。CNY及正负号沿用ETL源字段约定；各业务采用自己的出票、申请、改签出票、增值创建日期。航司保持源业务取值差异。返点/后返、实际结算、ADM、汇率、增值退款完整性尚未由此表证明；业务估算不等同已结算，高风险标签或原因不在该表，不作稳定收益或责任结论。

## 部署与验证

沿用现有Hive连接环境变量，无需改DATA_MODE为hive；服务器正常构建API和web镜像即可，端口1818/5160不变。本地更新API时只复制新增服务和routes再重启现有容器，保留容器已有连接配置；前端单独重建。无新增ETL、建表、数据写入或自动Git推送。

新增离线测试 `backend/tests/test_comprehensive_analysis.py`，覆盖四类coverage、真实零/缺日期、混合批次、计数一致性、NULL利润、Decimal、产品平台命名空间、交叉筛选、ADS查询范围、异常无演示回退和API校验。
